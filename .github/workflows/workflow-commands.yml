name: Workflow commands

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Display warning and error 
        run: |
          echo "::warning::Warning message"
          echop "::error::Error message"
